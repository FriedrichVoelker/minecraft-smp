name: ${project.name}
prefix: ${project.name}-${project.version}
version: ${project.version}
authors: [Friedrich (xDragonTaX)]
website: https://enten.dev
main: dev.enten.smp.Main
api-version: 1.20

commands:

   